spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create-drop

  flyway:
    enabled: false

jwt:
  secret: OMGItsQuestEngineV2JWTSuperSecureTokenMadeByDN
  expiration: 86400000
