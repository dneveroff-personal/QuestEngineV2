# API

Статус: **TBD** — будет заполнено на этапе "API-контракт и NFR".

Планируется:
- `conventions.md` — формат ошибок, пагинация, версионирование, аутентификация.
- `endpoints.md` — обзор REST-ресурсов (или ссылка на генерируемую OpenAPI-спеку из `SwaggerConfig`).
